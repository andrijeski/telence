{
  "version": "1.0.0",
  "description": "Telegram bot integrating LLMs (OpenAI & Gemini) using Deno and Grammy",
  "author": "Sasho Andrijeski",
  "license": "ISC",
  "tasks": {
    "start": "deno run --allow-net --allow-read --allow-write --allow-import --allow-env src/main.ts",
    "lint": "deno lint",
    "test": "deno test",
    "fmt": "deno fmt"
  },
  "compilerOptions": {
    "lib": ["ESNext", "DOM"],
    "strict": true
  }
}
